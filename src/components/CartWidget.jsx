const CartWidget = () => {
    return (
        <div className="cart-widget">
            🛒
            <span className="cart-notification">0</span>
        </div>
    );
};

export default CartWidget;